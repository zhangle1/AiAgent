# 聊天文件引用与右侧定位规格

## 目标

当 Codex 本地代理或自有 Agent Loop 在回答中涉及代码文件时，用户可以点击文件引用，在聊天页右侧打开对应文件，并定位到具体行。该能力复用现有的右侧代码查看器，不直接暴露服务器任意磁盘文件。

## 范围

- 支持 Codex 本地代理完成任务后返回的已修改文件。
- 支持自有 Agent Loop 的代码库检索引用，以及回答中显式给出的文件路径。
- 支持 `.cs`、`.ts`、`.tsx`、`.js`、`.json`、`.sql`、`.md`、`.yml` 等常见文本源码与配置文件。
- 支持可选行号；打开后高亮该行并滚动到可视区域中央。

不在本期范围内：编辑文件、打开本机 IDE、访问未挂载代码库中的文件、二进制文件预览。

## 统一文件引用

内部统一使用如下 Markdown 链接协议：

```text
aiagent://code-file?path=<URL 编码的文件路径>&line=<可选正整数>
```

示例：

```markdown
[U9CheckStock.cs](aiagent://code-file?path=src%2FU9CheckStock.cs&line=86)
```

前端也会识别回答中的常见文件路径、Markdown 链接或行内代码（例如 `Services/U9CheckStock.cs:86`），再交给后端验证和解析；解析失败时保持原文，不打开右侧面板。

## 后端接口

`POST /api/v1/code-repositories/projects/{projectId}/resolve-file-reference`

请求：

```json
{ "reference": "E:\\项目\\demo\\src\\U9CheckStock.cs:86" }
```

成功响应：

```json
{
  "repository_name": "cpsapi",
  "file_path": "src/U9CheckStock.cs",
  "line": 86
}
```

- `404`：引用不存在、不是文本文件、路径歧义，或不属于该项目的已挂载代码库。
- `403`：当前用户无权访问项目。
- `400`：引用为空或格式无效。

右侧查看器继续使用既有的受限文件读取接口，以 `repository_name + file_path` 读取内容。

## 安全规则

1. 解析前必须验证当前用户对项目的访问权限。
2. 仅在该项目已登记的代码库根目录下解析文件。
3. 规范化路径后拒绝任何越出根目录的路径（包括 `..`、符号链接逃逸及其他绝对磁盘路径）。
4. 仅返回代码库名称、相对路径和行号，不返回服务器真实目录。
5. 文件名称检索仅在结果唯一时可用；多个同名文件返回未找到，避免错误打开。

## 生成与展示流程

1. Codex 任务结束时，把已修改文件附加为统一协议链接；原始绝对路径只作为解析输入，不直接显示或授权读取。
2. 自有 Agent Loop 的结构化代码检索引用使用已有的代码库名、相对路径与行号直接定位；回答中的显式文件路径则沿用同一解析接口。
3. `MarkdownMessage` 识别统一协议和可解析的文件引用，调用解析接口。
4. 解析成功后调用聊天页已有的 `openCodeFile`，展开右侧检查器的“文件”标签。
5. 右侧检查器加载文件、高亮行号并滚动定位。

## 验收标准

- Codex 修改一个已挂载代码库中的文件后，回答末尾显示可点击的“涉及文件”链接。
- 点击链接后右侧打开正确代码库中的正确文件；带行号时定位且高亮该行。
- Agent Loop 的代码检索引用可以打开相同的右侧文件查看器。
- 对其他项目、未挂载目录、`..` 路径及不存在文件的引用不会泄露或读取文件。
- 普通外部网页链接仍按原行为在新标签页打开。
