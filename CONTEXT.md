# AiAgent 工作台

AiAgent 是面向研发交付的 AI 工作台。它把项目、会话、代码与知识上下文、模板及模型能力组织为可追溯的协作空间；本词汇表统一桌面端和移动端使用的产品语言。

## 工作对象

**项目上下文**：
用户在一次 AI 协作中主动选择的项目范围，决定可用的代码库、知识与模板范围；它不是单纯的页面筛选条件。
_Avoid_: 项目筛选、项目标签

**会话**：
一段连续的用户与 AI 协作记录，可归属一个项目，也可暂不归属项目。
_Avoid_: 聊天窗口、任务、线程

**上下文来源**：
在发送前显式加入会话的代码库、知识库、文件或项目资料。
_Avoid_: 附件（附件仅指用户上传的文件）、背景资料

**提问模板**：
可复用的提示内容及其变量定义；填写变量后会生成一条待发送的会话输入。
_Avoid_: AI 技能、工作流

## 移动端交互

**工作台抽屉**：
手机端从左侧呼出的全局导航层，承载工作台入口、会话和工具入口；不是桌面侧边栏的缩放副本。
_Avoid_: 移动侧边栏、菜单页

**会话输入器**：
固定在聊天页面底部、用于编辑并发送消息的复合控件，包含上下文入口、附件入口与发送动作。
_Avoid_: 输入框、聊天框

**操作面板**：
从底部弹出的单一任务操作层，用于选择项目、上下文来源、模板变量或会话操作。
_Avoid_: 弹窗、下拉菜单
