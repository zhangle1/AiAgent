# 客户介绍手册事实核对（内部维护）

核对日期：2026-09-18。代码基线：`df7e4d2238187d05b737d86ef2e6b01ba4b9844e`。开始核对时工作区无已有未提交修改。本轮仅新增文档及文档构建工具，没有修改产品业务代码、运行配置或数据，也未拉取远端。这里的“最新”指本地当前代码基线，不代表已经核验远端或生产部署。

对外交付位于 `对外介绍-2026-09-18/`。本核对表和生成脚本不加入客户资料压缩包。原 2026-09-10 操作手册继续保留。

## 材料使用方式

- 原《坤伴平台操作手册》用于理解已有模块与操作路径，新版改按客户价值和交付评审组织。
- 原《知识库体系与坤伴 2.0 接入方案》及 `参考资料/2026-09-10-kunbuddy-1-to-2-roadmap.md` 用于区分建设方向与已有实现。
- 平台能力结论以当前后端实现及前端入口核对；没有把行业报告、Google/Claude 参考资料或 OpenViking 路线图作为平台已上线证据，未复用其内容或图表。
- 没有使用旧截图、实际会话、客户任务或运行日志。公开版用流程与能力表说明产品，避免传播截图中未经复核的信息。

## 主要表述与代码依据

所有路径相对于仓库根目录；行号以本次基线为准，未来维护优先使用类名和方法名定位。

| 公开版章节／表述 | 实现依据 | 核对结论与表述边界 |
| --- | --- | --- |
| 01–02：任务进入项目会话 | `backed/Services/Task/ProjectTaskAppService.cs`：任务图片准备、handoff、Import；`front/components/tasks/TaskBoardPageV2.tsx` | 有任务、项目关联与会话交接代码；不宣称任意外部任务平台免适配。 |
| 02：按阶段复用提示模板 | `backed/Services/PromptTemplate/PromptTemplateService.cs:27` 的 AllowedStages；`front/components/prompt-templates/PromptTemplateMarket.tsx` | 有需求、设计、开发、理解、测试、交付阶段；不将提示模板等同于已安装可执行 Skill。 |
| 03：画布职责、模型与 Skill | `backed/Services/WorkCanvas/WorkCanvasService.cs:93` 的 UpdateNode、BuildWorkflowExecution；`front/components/work-canvas/WorkCanvasPage.tsx` | 保存职责、模型和手填 Skill 列表；运行环境是否提供对应 Skill 另行判断。 |
| 03：依赖推进与历史 | 同文件 StartWorkflowRunNode、StartReadyWorkflowNodes 中的上游 completed 判断、ListWorkflowRuns；前端流程运行历史 | 所有上游完成后才推进；历史记录模型、状态、起止时间与失败；未宣称无人值守持久后台调度。 |
| 03：个人会话画布 | `WorkCanvasService.cs`：Owned、AddNode、Get 中的 UserId 筛选 | 属于个人有权访问的会话编排；不能宣传多人共同编辑画布或任意跨账户会话协作。 |
| 04：保留原文、按需提炼 | `backed/Services/Knowledge/KnowledgeAppService.cs:139` 的 CompileDocument、create/upload；KnowledgeIngestionService、KnowledgeWorkspaceService | 原文与派生知识分开；没有声称上传后自动完成全部知识治理。 |
| 04：两阶段与证据校验 | `backed/Services/Knowledge/Core/KnowledgeCompiler.cs:61`、`:84`、`:116` | 先分析再生成，逐段核对原文引用；证据匹配不等于语义正确性或人工审核完成。 |
| 04：任务队列与原文预览 | KnowledgeCompilationWorker、KnowledgeOfficePreviewService；`front/components/knowledge/KnowledgeTaskQueue.tsx` | 有阶段、进度、取消和重试；DOC/XLS 依赖服务器转换组件。 |
| 04：检索可含草稿 | `backed/Services/Knowledge/KnowledgeWikiRetrievalService.cs:48` 附近的响应提示；Core/KnowledgeWikiSearch | 不宣传“全部经过审核”或“知识回答必然正确”；目录分类不视为租户权限边界。 |
| 05：项目、图片与原型 | `front/components/prototype-studio/PrototypeStudio.tsx`、PsdFiles；`front/lib/prototype-psd.ts`；`backed/Services/Chat/Codex/CodexChatService.cs` | 图片能力按代理配置；PSD 为分组像素图层，不称原生文字图层。 |
| 05：看板版本保护与校验 | `backed/Services/DashboardApp/DashboardApplicationAppService.cs:289` ApplyPatchAsync、ValidateChangeAsync | 读取真实文件、ExpectedSha256 核对和静态检查；不替代业务测试与上线验收。 |
| 06：变更集审批和快照 | `backed/Services/CodeDelivery/CodeDeliveryAppService.cs:80` Approve、`:99` Deliver | 审批和交付检查 CanCommitCode；交付复核 SnapshotSha256，变化要求重新创建和审批。 |
| 06：普通自动校验范围 | `backed/Services/Git/GitWorkspaceService.cs:443` 检查集合 | repository、changes、upstream、remote、behind、diff_check；普通变更集没有通用编译或业务测试门禁。 |
| 06：版本记录与任务状态 | `CodeDeliveryAppService.cs:124` 附近 CommitSha、交付状态和本地任务 done 写回 | 是平台关联任务更新，不宣称外部工作项状态双向同步；多仓库可能部分成功，不承诺原子交付或自动回滚。 |
| 06、08：权限控制范围 | `backed/Services/Admin/AdminService.cs:19` ProjectAccessService；Codex sandbox 模式；CodeDeliveryAppService | 交付 API 门禁不是对所有 Git 或高权限 CLI 路径的不可绕过隔离；公开版明确服务账户、代理与仓库权限需同步管理。 |
| 07：养护副本、构建与审批分支 | `backed/Services/CodeRepository/RepositoryMaintenanceService.cs:182` 起的副本／构建／交付逻辑；`:290` Branch；`backed/Services/Git/GitWorkspaceService.Maintenance.cs` | 定时执行不自动批准；独立 maintenance 分支；交付复核权限、快照与基线。 |
| 07：构建支持范围 | `backed/Services/CodeRepository/RepositoryMaintenanceBuildService.cs:65` BuildAsync | .NET 及带 build 脚本、package-lock 的 npm 项目；没有泛化为任意语言或任意构建体系。 |
| 08：账户、审计与用量 | AdminService、AdminAppService；`backed/Services/Usage/UsageStatisticsService.cs:34` 和 IsEstimated；UsageSettingsPage | 有账户管理、会话审计和用量聚合；部分 Token 估算，不是精确结算或完整经营效能仪表盘。 |
| 08：工作项客户适配 | `backed/Services/Task/ProjectTaskAppService.cs:28` 企业常量 | 当前连接含特定企业配置；公开版不含企业内部标识，并明确其他客户需适配验收。 |
| 09–10：案例流程与试点指标 | 由已核对功能组合成建议使用流程 | 明确标注示例／建议；材料包与指标需要团队记录汇总，没有伪称真实客户案例、平台自动报表或实测提效。 |
| 10：后续演进 | 原手册与路线图中的知识资源寻址、跨系统 Agent 协作等 | 仅列方向，不计入本次已具备能力或交付承诺。 |

## 维护与重新生成

1. 在 `对外介绍-2026-09-18/坤伴Agent协同研发平台介绍手册.md` 修改正文；如产品行为改变，先重新核对相应实现。
2. 运行 `python docs/kunbuddy-platform-manual-2026-09-10/build_customer_manual.py` 生成 Word 与单文件 HTML。依赖 `python-docx`。
3. 运行 `python docs/kunbuddy-platform-manual-2026-09-10/check_customer_manual.py --browser <本机Chrome可执行文件>` 生成 PDF、执行文档检查并打包公开文件。依赖 `playwright`、`pymupdf`。浏览器无需访问运行中的平台或网络。
4. 检查 PDF 页数、分页、表格，以及临时目录中的封面／正文／移动端图像。Word 另检查 XML 结构、表格、书签；不同 Office 软件的换行分页可能不同，正式定版可用目标 Office 环境再导出核对。

公开 HTML 内联样式，无外部图片、脚本、字体请求，含目录锚点和移动端样式。此次生成可对外分发的离线文档，没有部署公网网站或改变平台访问权限。

## 本次交付检查结果

- PDF：12 页（封面、阅读目录、10 章正文），各章标题与内容页匹配，无孤立页脚页；抽查封面、交付治理、末章等页面排版。
- HTML：10 个目录链接与锚点匹配；浏览器检查未发起外部 HTTP 请求；390 像素宽度无横向溢出，并查看移动端正文效果。
- Word：文档包可读取，11 个表格、10 个章节书签及目录链接通过结构检查。当前环境未使用 Office 排版引擎验证 Word 的最终页数；客户固定排版转发优先使用 PDF。
- 公开正文：检查未包含内部工作区路径、源码路径、提交哈希、内部企业标识、凭据样式或占位内容。压缩包仅含四种公开正文格式。
- 文档变更检查：保留既有 README 的 UTF-8 无 BOM 与 CRLF；使用 `git -c core.whitespace=cr-at-eol diff --check` 通过格式检查。没有执行产品端到端验收，也没有把源码核对说成线上验证。
